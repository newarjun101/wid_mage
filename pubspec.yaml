name: wid_mage
description: A perfect library to convert widget to image with just a few lines of code.
version: 0.0.1
homepage: https://github.com/prolaymm/wid_mage.git

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

